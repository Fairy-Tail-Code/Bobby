目前的思路：基础能力交给合适的框架，沿用当前的基础设施，主要在于设计多agent的编排、交互、提示词。
目标，完成以下内容，并且由于处于快速实践阶段，大多数内容尽量做到可插拔且统一管理，

# 4/9

## 1 基础设施

沿用C:\Users\WUJIEAI\PycharmProjects\OpenHarness\openharness里的skill、mcp。

例如CLI、测试能力、部署能力、GIT能力等等

##  2 长时运行应用开发的 Harness 设计

> 作者：Prithvi Rajasekaran（Anthropic Labs 团队）

### 核心结论

用**生成器 + 评估器**的多智能体结构（借鉴 GAN 思想），可以显著突破单 Agent 在设计质量和长时编码任务上的天花板。最终形态是**三智能体架构（Planner → Generator → Evaluator）**，能在数小时的无人值守会话中产出完整的全栈应用。

---

### 两个关键问题

(要求：可插拔地实现以下两个能力——上下文重置&&上下文压缩)

#### 1. 上下文窗口失控（Context Anxiety）

模型在上下文接近上限时会提前收尾。解决方案是**上下文重置**：清空上下文、启动新 Agent，并通过结构化交接文件传递状态。这不同于 compaction（压缩历史摘要）——重置给 Agent 一个干净的起点，彻底消除"焦虑"。

> 注：Opus 4.6 已大幅改善该问题，新架构中可直接去掉重置机制，改用 SDK 的自动 compaction。

#### 2. 自我评估偏宽松

Agent 评价自己的输出时倾向于给高分，哪怕质量平庸。**将生成和评估分离**是核心解法——独立的 Evaluator Agent 更容易被调教成严格的审查者。

---

### 前端设计：让主观质量可量化

给 Generator 和 Evaluator 同一套评分标准（四个维度），让它们有共同的"品味语言"：

| 维度         | 含义                                                         | 权重                   |
| ------------ | ------------------------------------------------------------ | ---------------------- |
| **设计质量** | 色彩、排版、布局是否形成统一的视觉语言和氛围                 | 高                     |
| **原创性**   | 是否有定制化决策，还是模板/默认样式/AI 套路（如白卡+紫渐变） | 高                     |
| **工艺**     | 字体层级、间距、色彩对比等基本功                             | 低（模型默认表现已够） |
| **功能性**   | 用户能否理解界面并完成任务                                   | 低（模型默认表现已够） |

Evaluator 使用 **Playwright MCP** 直接操作运行中的页面进行评分，而非评估静态截图。

#### 循环流程

1. Generator 生成 HTML/CSS/JS 前端
2. Evaluator 浏览页面、截图、按四维打分并写出详细批评
3. 批评反馈给 Generator，Generator 根据得分趋势决定**精修**（趋势良好）还是**重构**（方向不对）
4. 重复 5~15 轮

---

### 全栈编码：三智能体架构

#### 架构设计

用户 1~4 句话的 Prompt ↓ 【Planner】 扩展为完整产品规格（功能列表、技术架构、视觉设计方向） 强调：高层交付物，不指定实现细节，主动植入 AI 功能 ↓ 【Generator】（按 Sprint 逐个特性构建） 技术栈：React + Vite + FastAPI + SQLite/PostgreSQL + Git 每个 Sprint 开始前与 Evaluator 协商"Sprint 合同"（明确验收标准） Sprint 结束后自评，再交给 Evaluator ↓ 【Evaluator】（每个 Sprint 后评审） 使用 Playwright MCP 点击运行中的应用 按合同标准验收；任一维度低于阈值则 Sprint 失败 输出具体 Bug 报告（指明文件行号、根因），Generator 修复后重跑

#### Sprint 合同机制（暂时不使用sprint合同机制，默认模型能力足够）

在每个 Sprint 开始前，Generator 提案"我要做什么、怎么验证"，Evaluator 审核后双方通过文件交换达成一致，避免规格歧义导致的方向偏差。

#### Evaluator 调教要点

开箱即用的模型是糟糕的 QA Agent——它会发现问题，然后自我说服"其实不严重"。需要反复阅读评估日志，找出 Evaluator 判断与人工判断的偏差点，针对性修改 Prompt，直到打分标准与预期对齐。

---

### 架构演进：去掉 Sprint 构件

随着 Opus 4.6 的发布，模型原生能力提升，Sprint 分解不再必要：

- **去掉 Sprint 构件**：Generator 可在单次连续会话中工作 2+ 小时不失控
- **Evaluator 改为端到端一次性 QA**：不再逐 Sprint 评审
- **Planner 和 Evaluator 保留**：仍有明显价值

> **核心原则**：Harness 的每个组件都是对"模型无法独立完成"的假设编码。模型越强，需要的脚手架越少。要定期重新审视哪些组件还是"承重墙"，哪些已经多余。

---

### 成本与时间参考

| 任务              | 架构                    | 耗时         | 费用 |
| ----------------- | ----------------------- | ------------ | ---- |
| 2D 复古游戏制作器 | 单 Agent                | 20 分钟      | $9   |
| 2D 复古游戏制作器 | 三智能体（含 Sprint）   | 6 小时       | $200 |
| 浏览器 DAW        | 三智能体（去掉 Sprint） | 3 小时 50 分 | $124 |

---

### 核心设计原则总结

1. **生成与评估分离**：是提升输出质量的最强杠杆
2. **主观质量可量化**：通过具体评分标准将"好不好"转化为"符不符合原则"
3. **评估器用实时交互，而非静态截图**：Playwright MCP 直接操作页面
4. **Planner 只定义 What，不规定 How**：避免高层错误级联到实现
5. **每次新模型发布，重新审视 Harness**：去掉不再承重的部分，为新能力留空间





## 3 代码结构设计

1. 至少解耦成3层——> 1 基础设施：存放mcp、skill。2  agent设计（即各个agent使用哪些工具、技能和它的风格设点，提示词）  3 agent编排。



### 4 框架选择

AG2

AG2文档地址：
https://docs.ag2.ai/latest/docs/user-guide/basic-concepts/introducing-group-chat/#understanding-the-group-chat-workflow

https://docs.ag2.ai/latest/docs/api-reference/autogen/Agent/

https://docs.ag2.ai/latest/docs/user-guide/basic-concepts/installing-ag2/

AG2源码地址：
C:\Users\WUJIEAI\PycharmProjects\OpenHarness\AG2\ag2



## 5 4/9 迭代完成记录

### 已完成
- ✅ 三层架构搭建（Infrastructure → Agent Design → Orchestration）
- ✅ 配置系统：LLM 从 .env 读取，MCP/Harness 从 YAML 读取
- ✅ MCP 管理器：异步连接、工具发现、生命周期管理
- ✅ MCP-AG2 工具桥：同步包装器，动态注册 MCP 工具为 AG2 Tool
- ✅ Skill 加载器：支持 SKILL.md 和 instruction.md，17 个 skill 已复制到项目
- ✅ Agent 工厂：Planner/Generator/Evaluator 创建 + skill 注入 + MCP 工具注册
- ✅ Swarm 编排：从 GroupChat auto 模式迁移到 Swarm handoff 模式
  - Planner → Generator（需求拆解完毕）
  - Generator → Evaluator（编码完成）
  - Evaluator → Generator/Planner（审核不通过/需求不足）
  - Evaluator → TERMINATE（审核通过）
- ✅ 33 个测试全部通过
- ✅ 9 个 MCP 服务器代码复制到项目本地
- ✅ 上下文管理接口（compaction 策略预留）

### 待完善（从需求文档中提取）
- [ ] Evaluator 调教：当前开箱即用的模型是糟糕的 QA Agent，需要反复阅读评估日志，找出偏差点修改 Prompt
- [ ] 上下文重置：当前只有 compaction，需要实现"清空上下文 + 结构化交接文件传递状态"的可插拔能力
- [ ] Sprint 合同机制（需求文档标注暂时不使用，默认模型能力足够）
- [ ] 实际运行验证：需要配置好 .env 和 MCP 服务器后跑一次完整流程


# 4/15

## 多角色邮件 HITL

### 需求
设计多个 UserProxyAgent，让不同的人负责不同的 agent（Planner/Generator/Evaluator），通过邮件：
1. 补充信息（需求模糊时）
2. 审批风险操作（删库、force push 等）
3. 确认审核决策

### 方案
- 1 个系统邮箱 + 3 个收件人（负责人各自邮箱）
- 邮件主题嵌入唯一 request_id 做匹配（QQ 邮箱会替换 Message-ID）
- `hitl.mode: email|stdin` 配置切换，完全向后兼容

### 已完成
- ✅ EmailUserProxyAgent 实现（SMTP 发送 + IMAP 轮询）
- ✅ 3 角色邮件代理创建和 Handoff 路由
- ✅ Agent 提示词更新（告知 LLM 有对应负责人可求助）
- ✅ 邮件匹配踩坑修复（request_id 主题匹配、UNSEEN 过滤、a_get_human_input 真实上下文提取）
- ✅ MCP shell_server subprocess stdin 死锁修复
- ✅ 配置系统（SmtpConfig、ImapConfig、HitlConfig）

### 待完善
- [ ] 端到端验证：配置真实 SMTP/IMAP 后跑一次完整邮件 HITL 流程
- [ ] 超时策略优化：当前超时后返回固定文本，可以改为让 agent 重试或降低操作风险等级
- [ ] 邮件模板美化：当前纯文本，可以加 HTML 格式

# 5/6

## Gateway 接入与 setup 简化

### 需求
- `harness setup` 和首次安装后的 setup 向导中，gateway 要可选 `feishu` 和 `weixin`
- 选择飞书后直接在终端输出 QR 码，扫码后自动创建应用；不再要求手填飞书相关 env
- 飞书登录/创建模式尽量复刻 `hermes-agent`
- 新增微信 gateway 选项，也走终端直接出 QR 码触发微信官方插件
- `harness server start` 不再写死飞书，而是按当前 gateway 配置启动
- `harness setup` 里 gateway 要支持连续勾选 `feishu`、`weixin`
- `harness server start` 需要支持同时启动飞书和微信两个 gateway
- 飞书、微信两侧消息都要汇入同一个 `SessionManager`
- 如果引入新依赖，要同步更新 `pyproject.toml`


# 5/9

## Bobby 记忆系统对齐 cc-haha

### 新需求

- 当前 `Bobby` 的记忆系统主要依赖 prompt + 文件读写能力，Agent 要先“意识到自己应该去存记忆”
- 需要参考 `cc-haha` 的实现，把记忆能力升级成系统级基础设施
- 先完成最小可用的长期记忆读写层，不要求本次就把 extract/dream 全部做完

### 本次落地范围

- 新增 `infrastructure/memory/` 模块
- 新增 `MemoryConfig`
- 为核心 Agent 注入 `MEMORY.md` 索引
- 为核心 Agent 注册 `load_memory` / `save_memory`
- 自动维护 `MEMORY.md`
- 兼容旧 `user_profile.md`
- 打通 `factory / agent_pool / swarm_session` 三条 Agent 创建链

### 暂不纳入本次

- 对话结束自动提取记忆
- dream consolidation
- 团队共享记忆同步

### 验收标准

- Agent 的 `system_message` 中可看到 `## Project Memory`
- Agent 可直接调用 `load_memory` / `save_memory`
- 保存记忆后会生成或更新 `MEMORY.md`
- 旧安装里的 `user_profile.md` 不会因为新结构而完全失效

## Bobby 自动记忆抽取

### 新需求

- 不能只依赖 Agent 在对话中临时想到 `save_memory`
- 需要在 session 收尾阶段自动复盘对话并提炼长期记忆

### 本次落地范围

- 在 completed / terminated session 后自动抽取 durable memory
- 复用现有 memory 文件格式与 `MEMORY.md`
- 支持通过配置关闭自动抽取
- 限制单次会话自动沉淀条数

### 验收标准

- 会话结束后可自动新增或更新 memory 文件
- 自动抽取出的内容会进入 `MEMORY.md`
- 可通过 `harness.memory.auto_extract_enabled` 控制开关

## 多 Agent 群聊模式迁移到 AG2 Beta Network

### 新需求

- 当前专家模式虽然已经从最早的 `GroupChatManager` 迁到 pattern/group 路径，但本质上仍是 group chat 风格
- 需要依据官方迁移文档，把当前多 agent 群聊模式完整迁到 AG2 beta network 思路
- 迁移后，agent 间协作不再依赖 `transfer_to_*` handoff tool 作为框架内建路由机制

### 本次落地范围

- 多 agent 专家模式底层改为 beta network runtime
- 保留现有 `single` 模式不动
- 保留 session snapshot、gateway/CLI 前端通知、memory extract、knowledge sync
- MCP / memory / skill 工具切为 beta tool 接入方式
- 用结构化 `next_step` 替代 handoff tool call

### 验收标准

- `swarm` 模式下不再依赖 `DefaultPattern + a_run_group_chat_iter`
- PM / Planner / Generator / Evaluator 可以继续串起完整流程
- 人工提问/审批仍可通过现有 channel 注入回复
- session 结束后仍可保存 snapshot，并继续进行 memory / knowledge 收尾
- 新增针对 beta network runtime 的测试覆盖主链路与非法路由
- 迁移完成后，删除无调用方的旧 group chat/swap handoff 实现，避免主仓库长期维护两套专家模式

## 自动记忆合并策略

### 新需求

- 自动抽取不能只依赖“同名覆盖”
- 当相关 memory 已存在时，需要先读取旧记忆并合并，再决定是否重写

### 本次落地范围

- 抽取到已有同名 memory 时先加载旧文档
- 通过 merge prompt 生成最终 memory 内容
- 如果合并结果与现有内容等价，则跳过写盘

### 验收标准

- 同名 memory 不再简单直接覆盖
- 新旧 durable facts 能合并进入同一份 memory
- 无实际变化时不会反复刷新 memory 文件

## 开发协作约束修正

### 新需求

- `git worktree` 仅作为开发时的协作习惯
- 不将 worktree 管理做成 `Bobby` 的默认 prompt、默认能力或默认测试要求

### 处理要求

- 回滚此前误加到项目内的 worktree 相关实现
- 保留已经完成的记忆系统改造

## 微信 Gateway HITL 跨事件循环修复

### 新需求

- 微信 gateway 在 AG2 Swarm 运行到 `a_get_human_input()` 时，向微信发送 HITL 提问会抛出：
  `RuntimeError: Timeout context manager should be used inside a task`
- 需要修复微信服务化链路里 AG2 线程事件循环与 gateway 主事件循环之间的资源跨 loop 访问问题
- 同时梳理 reply 注入链路，避免 `Future` 在创建 loop 之外被直接 `set_result()`

### 本次落地范围

- `WeixinBotService.send_text()` 改为在非主 loop 调用时自动切回 bot 主 loop 执行
- `ChannelWeixinService` 的 reply 注入和取消改为线程安全调度
- `ChannelFeishuService` 同步对齐 reply 注入逻辑，消除同类隐患
- 增加跨 loop 回归测试，覆盖发送桥接与 reply 注入

### 验收标准

- 微信 HITL 提问不再因为复用主 loop `aiohttp.ClientSession` 而报错
- AG2 子线程事件循环可安全触发微信发送
- 主 loop 收到用户回复后，可安全注入到等待中的 `Future`

## DeepSeek 专家模式结构化输出兼容修复

### 新需求

- 专家模式切到 AG2 beta network 后，角色 agent 使用 `response_schema=NetworkTurn`
- 当底层模型配置为 DeepSeek 兼容接口时，请求 `chat.completions` 会返回：
  `400 Bad Request: This response_format type is unavailable now`
- 需要保证专家模式在保留结构化路由 contract 的同时，兼容这类不支持 `response_format=json_schema` 的后端

### 本次落地范围

- 专家模式角色创建阶段识别 DeepSeek 类后端
- 支持后端继续使用原生 `response_schema`
- 不支持后端改为 prompt 约束 JSON 输出 + runtime 本地解析 `NetworkTurn`
- 增加回归测试，覆盖 schema 模式与纯文本 JSON 降级模式

### 验收标准

- 使用 DeepSeek 作为 PM / Planner / Generator / Evaluator 模型时，专家模式首轮不再因 `response_format` 报 400
- beta network runtime 仍能拿到可靠的 `message / next_step`
- 兼容后端不影响现有原生 schema 路径

## Session 壳层与 Runtime 分层重构

### 新需求

- 当前 `SwarmSession` 名称与职责不一致，既负责 `single` 又负责专家模式
- 同一个类里同时承载 session 生命周期和两套执行链路，维护成本过高
- 需要在不改变三层主思路的前提下，清理这部分历史包袱

### 本次落地范围

- `SwarmSession` 重构为 mode-agnostic 的 `AgentSession`
- `single` 模式执行逻辑迁入 `orchestration/single_runtime.py`
- 专家模式继续使用 `orchestration/network_runtime.py`
- 新增统一 `OrchestrationRunResult`
- `SessionManager` / cron / CLI / server 全部切到新 session 壳层

### 验收标准

- session 层不再直接内嵌 `_run_single_session()` / `_run_network_swarm()`
- `single` 与专家模式都仍可通过同一个 `SessionManager` 启动
- snapshot / resume / memory extract / knowledge sync 行为保持不丢
- 代码中不再保留 `infrastructure/session/swarm_session.py`







